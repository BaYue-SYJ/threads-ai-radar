# ============================================================
#  threads-ai-radar 依赖清单
# ------------------------------------------------------------
#  本项目刻意做成「零第三方依赖」，只使用 Python 标准库：
#      urllib  —— 抓取
#      sqlite3 —— 存储
#      http.server / socketserver —— API 与 SSE 服务
#      threading / json / re / math —— 其余
#
#  因此本文件是空的，不需要 pip install 就能直接运行。
#
#  这样做的原因：本项目要求「实时监控」，需要长时间稳定驻留运行。
#  依赖越少，越不容易因为环境、版本、代理问题而中断。
#
#  若后续要接入 AI 分析（把规则分类换成模型分类），
#  再按需在这里补充，例如：
#      requests>=2.32.0
# ============================================================
